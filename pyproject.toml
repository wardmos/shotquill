[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shotquill"
dynamic = ["version"]
description = "Cross-platform screenshot & annotation tool (macOS first)"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "wardmos" }]
keywords = ["screenshot", "annotation", "macos", "linux", "cli", "mcp", "qt", "pyside6"]
classifiers = [
    "Environment :: MacOS X",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Graphics :: Capture :: Screen Capture",
    "Topic :: Utilities",
]

dependencies = [
    "PySide6>=6.6",
    "pynput>=1.7",
    "pyobjc-framework-Cocoa>=10.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-Quartz>=10.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-ScreenCaptureKit>=10.0 ; sys_platform == 'darwin'",
    "pyobjc-framework-Vision>=10.0 ; sys_platform == 'darwin'",
]

[project.urls]
Homepage = "https://github.com/wardmos/shotquill"
Repository = "https://github.com/wardmos/shotquill"
Issues = "https://github.com/wardmos/shotquill/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-qt>=4.4",
    "ruff==0.15.15",
]

[project.scripts]
shotquill = "shotquill.cli:main"
squill = "shotquill.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "shotquill.__version__" }

[tool.ruff]
line-length = 100
src = ["src", "tests"]
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
